(()=>{const API='https://lavuq-bewerbung.lavuq.workers.dev';const q=new URLSearchParams(location.search),member=q.get('member'),token=q.get('token');const state=document.getElementById('state'),app=document.getElementById('app'),hello=document.getElementById('hello'),groupMeta=document.getElementById('groupMeta'),members=document.getElementById('members'),chatLog=document.getElementById('chatLog'),chatText=document.getElementById('chatText'),sendBtn=document.getElementById('sendBtn'),meetingLink=document.getElementById('meetingLink');function esc(s){return String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}function fmt(v){if(!v)return'';const d=new Date(v);return Number.isNaN(d.getTime())?'':new Intl.DateTimeFormat('de-DE',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit',timeZone:'Europe/Berlin'}).format(d);}function setState(t,kind=''){state.textContent=t;state.className='status'+(kind?' '+kind:'');}async function get(path){const u=new URL(API+path);u.searchParams.set('member',member||'');u.searchParams.set('token',token||'');const r=await fetch(u,{cache:'no-store'}),d=await r.json();if(!r.ok||!d.ok)throw new Error(d.code||'REQUEST_FAILED');return d;}async function loadStatus(){if(!member||!token){setState('Dein persönlicher Zugangslink ist unvollständig.','err');return;}try{const d=await get('/member-area-status');hello.textContent=`Hallo ${d.member.name} 👋`;groupMeta.textContent=`${d.group.code||'Deine LAVUQ-Gruppe'} · ${d.group.memberCount} Teilnehmer · Status: ${d.group.status||'Aktiv'}`;members.innerHTML=d.group.members.map(p=>`<div class="member${p.isMe?' me':''}"><strong>${esc(p.name)}</strong>${p.isMe?'<div class="muted">Du</div>':''}</div>`).join('');meetingLink.href=`termin.html?member=${encodeURIComponent(member)}&token=${encodeURIComponent(token)}`;app.classList.remove('hidden');setState('Dein Mitgliederbereich ist aktiv.','ok');await loadChat();}catch(e){setState('Der Mitgliederbereich ist für diesen Link aktuell nicht verfügbar.','err');}}async function loadChat(){try{const d=await get('/member-chat-list');chatLog.innerHTML=d.messages.length?d.messages.map(m=>`<div class="bubble${m.isMe?' me':''}${m.system?' system':''}"><strong>${esc(m.sender)}</strong><div>${esc(m.message).replace(/\n/g,'<br>')}</div><small>${fmt(m.sentAt)}</small></div>`).join(''):'<div class="muted">Noch keine Nachrichten. Schreib deiner Gruppe die erste Nachricht.</div>';chatLog.scrollTop=chatLog.scrollHeight;}catch(_){chatLog.innerHTML='<div class="muted">Der Chat konnte gerade nicht geladen werden.</div>';}}async function send(){const message=chatText.value.trim();if(!message)return;sendBtn.disabled=true;try{const r=await fetch(API+'/member-chat-send',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({memberId:member,token,message})});const d=await r.json();if(!r.ok||!d.ok)throw new Error(d.code||'SEND_FAILED');chatText.value='';await loadChat();}catch(_){setState('Die Nachricht konnte gerade nicht gesendet werden.','err');}finally{sendBtn.disabled=false;}}sendBtn.addEventListener('click',send);chatText.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send();}});loadStatus();setInterval(()=>{if(!app.classList.contains('hidden'))loadChat();},15000);})();