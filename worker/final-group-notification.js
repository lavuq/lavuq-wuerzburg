// LAVUQ – Abschlussnachricht nach manueller Kontaktfreigabe.
// Standard ist Dry-Run. Versand erfolgt nur mit dryRun=false + expliziter Bestaetigung.
const BASE_ID="apphnIBhuAbmMTUtY";
const MEMBERS_TABLE="tbl4QX0NIB3tUKtF4";
const APPLICANTS_TABLE="tblzLtbR5Yh4nR5aQ";
const MEMBER_GROUP="fldMUYzXykTpV0j2x";
const MEMBER_APPLICANT="fldcV8kd6KF7zdScE";
const MEMBER_STATUS="fldBS2hoKQX0Rr1aX";
const MEMBER_INVITE_STATUS="fldUmjMa2j7MLG5RA";
const MEMBER_CONTACT_SHARED="fld3LCPTEbAl46bF1";
const APPLICANT_NAME="fldPPnyiAKpIXnawY";
const APPLICANT_EMAIL="flduioUSJQ7BlM85W";
const APPLICANT_PHONE="fld0n0lHsBXYnPosF";
function text(v){if(v==null)return"";if(typeof v==="object"&&!Array.isArray(v)&&v.name)return String(v.name).trim();return String(v).trim();}
function firstLink(v){if(!Array.isArray(v)||!v.length)return null;const x=v[0];return typeof x==="string"?x:x?.id||null;}
function headers(env){return{Authorization:`Bearer ${env.AIRTABLE_TOKEN}`,"Content-Type":"application/json"};}
async function listMembers(env){let out=[],offset="";do{const p=new URLSearchParams({pageSize:"100",returnFieldsByFieldId:"true"});if(offset)p.set("offset",offset);const r=await fetch(`https://api.airtable.com/v0/${BASE_ID}/${MEMBERS_TABLE}?${p}`,{headers:headers(env)});if(!r.ok)throw new Error(`Airtable Mitglieder HTTP ${r.status}`);const j=await r.json();out.push(...(j.records||[]));offset=String(j.offset||"");}while(offset);return out;}
async function getApplicant(env,id){const r=await fetch(`https://api.airtable.com/v0/${BASE_ID}/${APPLICANTS_TABLE}/${id}?returnFieldsByFieldId=true`,{headers:headers(env)});if(!r.ok)throw new Error(`Airtable Bewerber HTTP ${r.status}`);return r.json();}
function groupLinked(m,groupId){const g=m?.fields?.[MEMBER_GROUP];return Array.isArray(g)&&g.some(x=>(typeof x==="string"?x:x?.id)===groupId);}
function currentAccepted(m){return text(m?.fields?.[MEMBER_STATUS])==="Aktiv"&&text(m?.fields?.[MEMBER_INVITE_STATUS])==="Angenommen";}
function esc(s){return String(s||"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));}
async function sendMail(env,to,name,others){if(!env.BREVO_API_KEY)throw new Error("BREVO_API_KEY fehlt");const senderEmail=env.BREVO_SENDER_EMAIL||"kontakt@lavuq-wue.de";const senderName=env.BREVO_SENDER_NAME||"LAVUQ Würzburg";const contacts=others.map(p=>`<li><strong>${esc(p.name)}</strong><br>E-Mail: ${esc(p.email)}${p.phone?`<br>Mobil/WhatsApp: ${esc(p.phone)}`:""}</li>`).join("");const html=`<div style="font-family:Arial,sans-serif;line-height:1.6;color:#0b1f3a;max-width:620px;margin:0 auto"><h2>Kontakt ${esc(name)},</h2><p>eure LAVUQ-Gruppe ist vollständig. Alle vier Mitglieder haben zugesagt und die Kontaktfreigabe wurde durch LAVUQ durchgeführt.</p><p><strong>Deine drei Gruppenmitglieder:</strong></p><ul>${contacts}</ul><p>Bitte stimmt euer erstes Treffen an einem öffentlichen Ort ab. Zu jedem Treffen darf eine Begleitperson mitgebracht werden.</p><p>Geht respektvoll mit den Kontaktdaten um und gebt sie nicht außerhalb eurer Gruppe weiter.</p><p>Viele Grüße<br><strong>LAVUQ Würzburg</strong></p></div>`;const r=await fetch("https://api.brevo.com/v3/smtp/email",{method:"POST",headers:{"api-key":env.BREVO_API_KEY,"Content-Type":"application/json",Accept:"application/json"},body:JSON.stringify({sender:{email:senderEmail,name:senderName},to:[{email:to,name}],subject:"Deine LAVUQ-Gruppe ist vollständig",htmlContent:html})});if(!r.ok)throw new Error(`Brevo HTTP ${r.status}`);}
export async function handleFinalGroupNotification(env,input={}){if(!env?.AIRTABLE_TOKEN)return{ok:false,status:500,code:"AIRTABLE_TOKEN_MISSING"};const groupId=String(input.groupId||"").trim();const dryRun=input.dryRun!==false;if(!/^rec[A-Za-z0-9]{14}$/.test(groupId))return{ok:false,status:400,code:"INVALID_GROUP_ID"};const all=await listMembers(env);const accepted=all.filter(m=>groupLinked(m,groupId)&&currentAccepted(m));if(accepted.length!==4)return{ok:false,status:409,code:"NOT_ALL_4_ACCEPTED",acceptedActiveCount:accepted.length};if(accepted.some(m=>m?.fields?.[MEMBER_CONTACT_SHARED]!==true))return{ok:false,status:409,code:"CONTACTS_NOT_RELEASED_FOR_ALL",contactReleaseReady:false};const people=[];for(const m of accepted){const applicantId=firstLink(m?.fields?.[MEMBER_APPLICANT]);if(!applicantId)return{ok:false,status:409,code:"APPLICANT_MISSING"};const a=await getApplicant(env,applicantId);const p={memberId:m.id,applicantId,name:text(a?.fields?.[APPLICANT_NAME])||"LAVUQ Teilnehmer",email:text(a?.fields?.[APPLICANT_EMAIL]),phone:text(a?.fields?.[APPLICANT_PHONE])};if(!p.email||!p.email.includes("@"))return{ok:false,status:409,code:"EMAIL_MISSING"};people.push(p);}const previews=people.map(p=>({recipient:{name:p.name,email:p.email},sharedContacts:people.filter(x=>x.memberId!==p.memberId).map(x=>({name:x.name,email:x.email,phonePresent:Boolean(x.phone)}))}));if(dryRun)return{ok:true,status:200,state:"READY_FOR_FINAL_GROUP_NOTIFICATION",dryRun:true,groupId,acceptedActiveCount:4,contactsReleasedForAll:true,wouldSendEmails:4,previews,emailsSent:0,airtableChanged:false};if(String(input.confirmation||"")!=="ABSCHLUSSNACHRICHT_SENDEN")return{ok:false,status:409,code:"EXPLICIT_CONFIRMATION_REQUIRED"};for(const p of people)await sendMail(env,p.email,p.name,people.filter(x=>x.memberId!==p.memberId));return{ok:true,status:200,state:"FINAL_GROUP_NOTIFICATION_SENT",dryRun:false,groupId,emailsSent:4,contactsReleasedForAll:true,airtableChanged:false};}
